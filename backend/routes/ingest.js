const express = require('express');
const router = express.Router();

// Placeholder for ingest route
router.post('/', (req, res) => {
  res.json({ message: 'Ingest route placeholder' });
});

module.exports = router;
